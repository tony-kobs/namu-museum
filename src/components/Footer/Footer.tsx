import styles from "./Footer.module.css";

export function Footer() {
  return (
    <footer id="contacts" className={styles.footer}>
      {/* TODO: Logo, SocialLinks, ScheduleList, ContactInfo, NavLink — з @/components/ui та @/data */}
    </footer>
  );
}

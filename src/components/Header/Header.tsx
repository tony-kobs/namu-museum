import styles from "./Header.module.css";

export function Header() {
  return (
    <header className={styles.header}>
      {/* TODO: Logo, IconButton (burger), HeaderMenu — з @/components/ui та @/data */}
    </header>
  );
}

# Utils

Чисті функції без side effects.

Планові утиліти:

- `formatPhone.ts` — форматування телефону
- `scrollToSection.ts` — smooth scroll до anchor
- `validateEmail.ts` — валідація email для підписки
